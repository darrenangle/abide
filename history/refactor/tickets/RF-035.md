# RF-035: Add Fixture-Driven Line-Add/Drop Mutation Regressions For Core Fixed Forms

- Priority: `P1`
- Status: `done`
- Area: `cross-form adversarial coverage`
- Origin: `EDGE-CASE-SWEEP-2026-04-01`

## Problem

The repo now has many targeted false-positive regressions, but a broad recurring class still deserves generated coverage: canonical fixed forms should not keep passing when a single extra line is appended or a defining line is removed from an otherwise valid exemplar.

## Primary Files

- `tests/forms/`
- `tests/fixtures/poems.py`

## Acceptance Criteria

- Add a shared mutation harness over representative canonical fixtures for core fixed forms.
- For each case, assert the baseline poem passes and the add-line/drop-line mutations fail canonically.
- Cover at least the high-value line-exact forms already used in training/evaluation work.

## Completed

- Added a shared fixture-driven mutation harness for core fixed forms using canonical poem fixtures.
- Asserted that each baseline passes, then that both add-line and drop-line mutations fail canonically.
- Verified with `uv run ruff check src/abide tests scripts`, `uv run mypy src/abide`, and `uv run pytest -q -W error` -> `626 passed, 1 skipped`.
