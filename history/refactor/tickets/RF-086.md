# RF-086: Run a Large W&B-Backed `rl_default` Gemma 4 E2B Prime-RL Training Pass

- Priority: `P2`
- Status: `done`
- Area: `training execution`
- Origin: `user request`
- Depends On: `RF-083`, `RF-084`

## Problem

The repo has a validated modern Gemma 4 E2B `prime-rl` path and a durable
`well_known` baseline, but the user now wants a larger real run that expands
to the broader `rl_default` subset: the original well-known forms plus the
lesser-known rollout forms (`Triolet`, `Pantoum`, `TerzaRima`, `Rondeau`,
`Clerihew`, and `BluesPoem`).

The existing runner profiles are biased toward the validated `well_known`
recipes. This task is to stage and launch a larger mixed-form job with heavier
rollout counts, record the exact runtime recipe, and capture the resulting
artifact and telemetry paths.

## Primary Files

- `scripts/run_prime_rl_gemma4_e2b.sh`
- `scripts/train_prime_rl.py`
- `history/refactor/`

## Acceptance Criteria

- A tracked large-run recipe is defined for the modern Gemma 4 E2B path using
  `form_set=rl_default`.
- The launched run uses W&B online logging rather than local offline-only mode.
- The run uses a heavier rollout budget than the existing `mixed-stable`
  baseline and records the exact launch parameters in the tracker.
- The ticket records the output directory and any directly observed early
  metrics or blockers.

## Superseded Probe

- Output: `models/prime_rl_gemma4_e2b_rl_default_large_rf086`
- W&B run: `rf086-rl-default-large`
- W&B URL:
  `https://wandb.ai/darren-angle-tomorrow-computing-company/abide-prime-rl/runs/c507437882d145f7b142f8ec57727578`
- Parameters: `max_steps=32`, `num_prompts=256`, `batch_size=4`,
  `rollouts_per_example=4`, `max_tokens=512`, `seq_len=1536`,
  `learning_rate=5e-6`, `max_async_level=1`
- Derived volume: `128` total rollouts
- Outcome: intentionally stopped after proving the recipe worked in practice;
  it reached step `4` cleanly with peak GPU memory around `20.4 GiB`.

## Active Launch Recipe

- Runner: `scripts/run_prime_rl_gemma4_e2b.sh`
- Model: `google/gemma-4-E2B-it`
- Form set: `rl_default`
- Output: `models/prime_rl_gemma4_e2b_rl_default_1024_rf086`
- W&B project: `abide-prime-rl`
- W&B run: `rf086-rl-default-1024`
- W&B URL:
  `https://wandb.ai/darren-angle-tomorrow-computing-company/abide-prime-rl/runs/e89911b6b7174bce869342e3dc3ecd53`

Resolved launch parameters:

- `max_steps=256`
- `num_prompts=256`
- `batch_size=4`
- `rollouts_per_example=4`
- `max_tokens=512`
- `seq_len=1536`
- `learning_rate=5e-6`
- `max_async_level=1`
- GPUs: inference `1`, training `0`

Derived training volume:

- `1024` total rollouts
- `256` prompt groups
- `4` rollouts per prompt group

## Outcome

- The `1024`-rollout run completed successfully through step `255` and shut down cleanly.
- Final artifacts:
  - Output: `models/prime_rl_gemma4_e2b_rl_default_1024_rf086`
  - Summary: `models/prime_rl_gemma4_e2b_rl_default_1024_rf086/run_default/run-e89911b6b7174bce869342e3dc3ecd53/final_summary.json`
  - W&B: `https://wandb.ai/darren-angle-tomorrow-computing-company/abide-prime-rl/runs/e89911b6b7174bce869342e3dc3ecd53`
- Directly observed end-of-run metrics from the final summary snapshot:
  - `step=255`
  - `progress/total_samples=1024`
  - `progress/total_problems=256`
  - `reward/all/mean=0.6702`
  - `time/step=47.84s`
  - `time/update_weights=11.79s`
- Trainer logs stayed stable through the end at about `20.5 GiB` peak GPU memory.
- The final summary is a last-window snapshot, not a full holdout evaluation; follow-up evaluation is tracked separately under the hard-form curriculum work.
