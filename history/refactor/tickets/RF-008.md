# RF-008: Audit Remaining Form Claims After Core Fixes

- Priority: `P2`
- Status: `done`
- Area: `follow-up audit`
- Origin: `AUDIT-002`

## Problem

The current audit found multiple places where descriptions overclaim verified behavior. There are likely more outside the first repaired set.

The follow-up audit should operate by variant family, not just by individual class, so shared edge cases are enforced consistently.

## Acceptance Criteria

- Re-audit remaining form families after `RF-003` and `RF-004`.
- Convert any remaining mismatches into concrete tickets or fix them directly.
- Update the board and worklog with the narrowed backlog.

## Notes

- Partially addressed in the current sweep:
  - narrowed `CurtalSonnet` and `Terzanelle` descriptions to the checks actually implemented
  - removed the `Elegiac` description's false claim that the verifier checks classical hexameter/pentameter directly
  - clarified `Distich` as a syllable/rhyme proxy rather than an elegiac-meter verifier
- Completed in the follow-up sweep:
  - `sapphic.py` now describes and enforces the English 11-11-11-5 proxy without implying classical quantitative scansion
  - historical meter notes in `burns.py`, `world.py`, `epigram.py`, `extended_sonnet.py`, and `ottava_rima.py` were narrowed or rewritten so they no longer overstate verifier coverage
  - the remaining search hits are intentional: `BlankVerse` strict-meter mode, the already-caveated `Quatrain` note, and an internal sonnet code comment
