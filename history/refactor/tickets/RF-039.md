# RF-039: Expand The Fixed-Form Mutation Harness To Additional Stable Fixture Families

- Priority: `P2`
- Status: `done`
- Area: `cross-form mutation coverage`
- Origin: `FOLLOWUP-035`

## Problem

The shared add-line/drop-line mutation harness now covers a useful core, but additional stable families with canonical fixtures still sit outside it. Expanding the harness increases the baseline coverage without introducing bespoke one-off tests.

## Primary Files

- `tests/forms/`
- `tests/fixtures/poems.py`

## Acceptance Criteria

- Add at least the stable `Limerick` and generic `Sonnet` fixture cases to the shared mutation harness.
- Assert the baseline fixture passes and the add-line/drop-line mutations fail canonically.
- Keep the suite green under the strict warning gate.

## Completed

- Added stable `Limerick` and generic `Sonnet` fixture cases to the shared add-line/drop-line mutation harness.
- Asserted that each baseline passes and the add-line/drop-line mutations fail canonically.
- Verified with `uv run ruff check src/abide tests scripts`, `uv run mypy src/abide`, and `uv run pytest -q -W error` -> `635 passed, 1 skipped`.
