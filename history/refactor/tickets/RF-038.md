# RF-038: Add Exported-Form Surface Smoke Tests For Instantiate/Describe/Verify Stability

- Priority: `P2`
- Status: `done`
- Area: `public form surface stability`
- Origin: `SURFACE-SWEEP-2026-04-01`

## Problem

The repo now instantiates the full exported form surface, but there is no standing regression that asserts all exported forms can be instantiated, described, and verified against representative generic inputs without runtime exceptions.

## Primary Files

- `tests/forms/`
- `src/abide/forms/catalog.py`

## Acceptance Criteria

- Add a smoke test that covers every exported form returned by `load_form_instances()`.
- Assert that `describe()` returns text and `verify()` returns sane `VerificationResult` objects on a small bank of generic inputs.
- Keep the test lightweight enough for the normal suite.

## Completed

- Added a smoke test over the full exported form surface returned by `load_form_instances()`.
- Asserted that every form provides a non-empty description and returns sane `VerificationResult` objects for representative generic inputs.
- Verified with `uv run ruff check src/abide tests scripts`, `uv run mypy src/abide`, and `uv run pytest -q -W error` -> `631 passed, 1 skipped`.
